# Independent WorkflowPulse product on playground.
# NOT a location under workflow.conversely.in.
#
# Until DNS for pulse.conversely.in exists, use direct port:
#   http://142.93.213.101:3200/
# Open with: ufw allow 3200/tcp
#
# When DNS is ready, enable this server block + certbot:

# server {
#     listen 80;
#     listen [::]:80;
#     server_name pulse.conversely.in;
#
#     location / {
#         proxy_pass http://127.0.0.1:3200;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
